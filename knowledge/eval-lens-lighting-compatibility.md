# 镜头与光源：兼容性

## 适用范围与资料边界
本文服务于 EquipForge 的机器视觉成像链路设计选型演示，重点是核对机械、电气、通信、软件和环境条件能否组成可落地方案。这是合成的演示快照，不对应任何真实厂商、型号、报价或库存，也不能替代现场测试、产品手册、安全评审与工程签字。

## 需求输入
处理镜头与光源兼容性需求时，先记录任务对象、工作节拍、现场空间、环境条件、现有系统、预算上限和验收方式。对于镜头与光源兼容性中的高精度、高速、稳定等模糊表述，必须追问可测量的阈值及测试条件，再决定是否进入候选比较。

## 核心参数检查
### 1. 视野、工作距离和目标尺寸
在金属表面划痕检测中执行镜头与光源兼容性并核对“视野、工作距离和目标尺寸”时，应保存数值、单位、上下限和测量前提。镜头与光源兼容性不得只依据标题关键词判断满足条件；字段缺失应标为待确认，候选排序需要说明该参数如何影响任务目标。

### 2. 焦距与放大倍率
在透明件轮廓测量中执行镜头与光源兼容性并核对“焦距与放大倍率”时，应保存数值、单位、上下限和测量前提。镜头与光源兼容性不得只依据标题关键词判断满足条件；字段缺失应标为待确认，候选排序需要说明该参数如何影响任务目标。

### 3. 接口和成像圈
在平面字符均匀照明中执行镜头与光源兼容性并核对“接口和成像圈”时，应保存数值、单位、上下限和测量前提。镜头与光源兼容性不得只依据标题关键词判断满足条件；字段缺失应标为待确认，候选排序需要说明该参数如何影响任务目标。

### 4. 景深与光圈
在小尺寸零件精密成像中执行镜头与光源兼容性并核对“景深与光圈”时，应保存数值、单位、上下限和测量前提。镜头与光源兼容性不得只依据标题关键词判断满足条件；字段缺失应标为待确认，候选排序需要说明该参数如何影响任务目标。

### 5. 畸变和分辨能力
在金属表面划痕检测中执行镜头与光源兼容性并核对“畸变和分辨能力”时，应保存数值、单位、上下限和测量前提。镜头与光源兼容性不得只依据标题关键词判断满足条件；字段缺失应标为待确认，候选排序需要说明该参数如何影响任务目标。

### 6. 光源形态与照射角度
在透明件轮廓测量中执行镜头与光源兼容性并核对“光源形态与照射角度”时，应保存数值、单位、上下限和测量前提。镜头与光源兼容性不得只依据标题关键词判断满足条件；字段缺失应标为待确认，候选排序需要说明该参数如何影响任务目标。

### 7. 波段、亮度与均匀性
在平面字符均匀照明中执行镜头与光源兼容性并核对“波段、亮度与均匀性”时，应保存数值、单位、上下限和测量前提。镜头与光源兼容性不得只依据标题关键词判断满足条件；字段缺失应标为待确认，候选排序需要说明该参数如何影响任务目标。

### 8. 频闪响应和控制方式
在小尺寸零件精密成像中执行镜头与光源兼容性并核对“频闪响应和控制方式”时，应保存数值、单位、上下限和测量前提。镜头与光源兼容性不得只依据标题关键词判断满足条件；字段缺失应标为待确认，候选排序需要说明该参数如何影响任务目标。

## 兼容性清单
### C1. 相机传感器尺寸
围绕镜头与光源兼容性的相机传感器尺寸建立输入端、输出端和约束条件三列表。镜头与光源兼容性中接口名称一致并不自动代表可互操作，还要确认物理层、信号范围、协议版本、数据格式、时序以及必要附件；不能确认的项目进入联调验证单。

### C2. 镜头接口与后焦
围绕镜头与光源兼容性的镜头接口与后焦建立输入端、输出端和约束条件三列表。镜头与光源兼容性中接口名称一致并不自动代表可互操作，还要确认物理层、信号范围、协议版本、数据格式、时序以及必要附件；不能确认的项目进入联调验证单。

### C3. 光源控制器通道
围绕镜头与光源兼容性的光源控制器通道建立输入端、输出端和约束条件三列表。镜头与光源兼容性中接口名称一致并不自动代表可互操作，还要确认物理层、信号范围、协议版本、数据格式、时序以及必要附件；不能确认的项目进入联调验证单。

### C4. 触发电平和时序
围绕镜头与光源兼容性的触发电平和时序建立输入端、输出端和约束条件三列表。镜头与光源兼容性中接口名称一致并不自动代表可互操作，还要确认物理层、信号范围、协议版本、数据格式、时序以及必要附件；不能确认的项目进入联调验证单。

### C5. 安装空间与散热
围绕镜头与光源兼容性的安装空间与散热建立输入端、输出端和约束条件三列表。镜头与光源兼容性中接口名称一致并不自动代表可互操作，还要确认物理层、信号范围、协议版本、数据格式、时序以及必要附件；不能确认的项目进入联调验证单。

## 常见风险与处置
### R1. 先选镜头再确认视野
“先选镜头再确认视野”会使镜头与光源兼容性产生表面匹配、实际不可用的结果。镜头与光源兼容性的处置方法是回到任务工况补齐证据，给出验证步骤和不通过时的替代边界；没有测试或字段支持时，不作确定性推荐。

### R2. 忽略传感器靶面造成暗角
“忽略传感器靶面造成暗角”会使镜头与光源兼容性产生表面匹配、实际不可用的结果。镜头与光源兼容性的处置方法是回到任务工况补齐证据，给出验证步骤和不通过时的替代边界；没有测试或字段支持时，不作确定性推荐。

### R3. 反光表面照明角度不当
“反光表面照明角度不当”会使镜头与光源兼容性产生表面匹配、实际不可用的结果。镜头与光源兼容性的处置方法是回到任务工况补齐证据，给出验证步骤和不通过时的替代边界；没有测试或字段支持时，不作确定性推荐。

### R4. 景深不足导致边缘失焦
“景深不足导致边缘失焦”会使镜头与光源兼容性产生表面匹配、实际不可用的结果。镜头与光源兼容性的处置方法是回到任务工况补齐证据，给出验证步骤和不通过时的替代边界；没有测试或字段支持时，不作确定性推荐。

### R5. 频闪亮度与占空比超限
“频闪亮度与占空比超限”会使镜头与光源兼容性产生表面匹配、实际不可用的结果。镜头与光源兼容性的处置方法是回到任务工况补齐证据，给出验证步骤和不通过时的替代边界；没有测试或字段支持时，不作确定性推荐。

## 决策与验收流程
镜头与光源兼容性的第一步是冻结硬约束并排除不满足项；第二步按核对机械、电气、通信、软件和环境条件能否组成可落地方案比较剩余候选；第三步核对附件、集成和维护成本；第四步输出首选、备选和待确认项；第五步在代表性工况下执行小规模验证，记录参数、现象与通过标准。

## 推荐输出格式
EquipForge 应依次展示镜头与光源兼容性需求摘要、候选参数表、兼容性差异、风险提示、样例预算以及验证计划。镜头与光源兼容性中的价格仅作为合成比较字段，库存仅作为流程演示字段；证据冲突时以用户提供的最新可核验资料为准。
