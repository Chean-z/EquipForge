# 工业相机：场景拆解

## 适用范围与资料边界
本文服务于 EquipForge 的机器视觉采集与在线检测选型演示，重点是把自然语言需求转成工况、任务目标、节拍、预算和部署边界。这是合成的演示快照，不对应任何真实厂商、型号、报价或库存，也不能替代现场测试、产品手册、安全评审与工程签字。

## 需求输入
处理工业相机场景拆解需求时，先记录任务对象、工作节拍、现场空间、环境条件、现有系统、预算上限和验收方式。对于工业相机场景拆解中的高精度、高速、稳定等模糊表述，必须追问可测量的阈值及测试条件，再决定是否进入候选比较。

## 核心参数检查
### 1. 成像方式与快门类型
在流水线外观缺陷检测中执行工业相机场景拆解并核对“成像方式与快门类型”时，应保存数值、单位、上下限和测量前提。工业相机场景拆解不得只依据标题关键词判断满足条件；字段缺失应标为待确认，候选排序需要说明该参数如何影响任务目标。

### 2. 有效分辨率与像元尺寸
在机械臂定位引导中执行工业相机场景拆解并核对“有效分辨率与像元尺寸”时，应保存数值、单位、上下限和测量前提。工业相机场景拆解不得只依据标题关键词判断满足条件；字段缺失应标为待确认，候选排序需要说明该参数如何影响任务目标。

### 3. 帧率、曝光和触发
在字符与条码识别中执行工业相机场景拆解并核对“帧率、曝光和触发”时，应保存数值、单位、上下限和测量前提。工业相机场景拆解不得只依据标题关键词判断满足条件；字段缺失应标为待确认，候选排序需要说明该参数如何影响任务目标。

### 4. 动态范围与信噪表现
在高速运动目标抓拍中执行工业相机场景拆解并核对“动态范围与信噪表现”时，应保存数值、单位、上下限和测量前提。工业相机场景拆解不得只依据标题关键词判断满足条件；字段缺失应标为待确认，候选排序需要说明该参数如何影响任务目标。

### 5. 镜头接口与传感器靶面
在流水线外观缺陷检测中执行工业相机场景拆解并核对“镜头接口与传感器靶面”时，应保存数值、单位、上下限和测量前提。工业相机场景拆解不得只依据标题关键词判断满足条件；字段缺失应标为待确认，候选排序需要说明该参数如何影响任务目标。

### 6. 数据接口与传输距离
在机械臂定位引导中执行工业相机场景拆解并核对“数据接口与传输距离”时，应保存数值、单位、上下限和测量前提。工业相机场景拆解不得只依据标题关键词判断满足条件；字段缺失应标为待确认，候选排序需要说明该参数如何影响任务目标。

### 7. 供电方式与功耗
在字符与条码识别中执行工业相机场景拆解并核对“供电方式与功耗”时，应保存数值、单位、上下限和测量前提。工业相机场景拆解不得只依据标题关键词判断满足条件；字段缺失应标为待确认，候选排序需要说明该参数如何影响任务目标。

### 8. 防护、温度和安装尺寸
在高速运动目标抓拍中执行工业相机场景拆解并核对“防护、温度和安装尺寸”时，应保存数值、单位、上下限和测量前提。工业相机场景拆解不得只依据标题关键词判断满足条件；字段缺失应标为待确认，候选排序需要说明该参数如何影响任务目标。

## 兼容性清单
### C1. 镜头像面和接口
围绕工业相机场景拆解的镜头像面和接口建立输入端、输出端和约束条件三列表。工业相机场景拆解中接口名称一致并不自动代表可互操作，还要确认物理层、信号范围、协议版本、数据格式、时序以及必要附件；不能确认的项目进入联调验证单。

### C2. 光源触发与控制
围绕工业相机场景拆解的光源触发与控制建立输入端、输出端和约束条件三列表。工业相机场景拆解中接口名称一致并不自动代表可互操作，还要确认物理层、信号范围、协议版本、数据格式、时序以及必要附件；不能确认的项目进入联调验证单。

### C3. 主机带宽和驱动
围绕工业相机场景拆解的主机带宽和驱动建立输入端、输出端和约束条件三列表。工业相机场景拆解中接口名称一致并不自动代表可互操作，还要确认物理层、信号范围、协议版本、数据格式、时序以及必要附件；不能确认的项目进入联调验证单。

### C4. 视觉软件与图像格式
围绕工业相机场景拆解的视觉软件与图像格式建立输入端、输出端和约束条件三列表。工业相机场景拆解中接口名称一致并不自动代表可互操作，还要确认物理层、信号范围、协议版本、数据格式、时序以及必要附件；不能确认的项目进入联调验证单。

### C5. 产线编码器或外部触发
围绕工业相机场景拆解的产线编码器或外部触发建立输入端、输出端和约束条件三列表。工业相机场景拆解中接口名称一致并不自动代表可互操作，还要确认物理层、信号范围、协议版本、数据格式、时序以及必要附件；不能确认的项目进入联调验证单。

## 常见风险与处置
### R1. 只看像素忽略视野精度
“只看像素忽略视野精度”会使工业相机场景拆解产生表面匹配、实际不可用的结果。工业相机场景拆解的处置方法是回到任务工况补齐证据，给出验证步骤和不通过时的替代边界；没有测试或字段支持时，不作确定性推荐。

### R2. 运动目标使用不合适的快门
“运动目标使用不合适的快门”会使工业相机场景拆解产生表面匹配、实际不可用的结果。工业相机场景拆解的处置方法是回到任务工况补齐证据，给出验证步骤和不通过时的替代边界；没有测试或字段支持时，不作确定性推荐。

### R3. 带宽不足导致丢帧
“带宽不足导致丢帧”会使工业相机场景拆解产生表面匹配、实际不可用的结果。工业相机场景拆解的处置方法是回到任务工况补齐证据，给出验证步骤和不通过时的替代边界；没有测试或字段支持时，不作确定性推荐。

### R4. 镜头成像圈覆盖不足
“镜头成像圈覆盖不足”会使工业相机场景拆解产生表面匹配、实际不可用的结果。工业相机场景拆解的处置方法是回到任务工况补齐证据，给出验证步骤和不通过时的替代边界；没有测试或字段支持时，不作确定性推荐。

### R5. 现场照明变化未留余量
“现场照明变化未留余量”会使工业相机场景拆解产生表面匹配、实际不可用的结果。工业相机场景拆解的处置方法是回到任务工况补齐证据，给出验证步骤和不通过时的替代边界；没有测试或字段支持时，不作确定性推荐。

## 决策与验收流程
工业相机场景拆解的第一步是冻结硬约束并排除不满足项；第二步按把自然语言需求转成工况、任务目标、节拍、预算和部署边界比较剩余候选；第三步核对附件、集成和维护成本；第四步输出首选、备选和待确认项；第五步在代表性工况下执行小规模验证，记录参数、现象与通过标准。

## 推荐输出格式
EquipForge 应依次展示工业相机场景拆解需求摘要、候选参数表、兼容性差异、风险提示、样例预算以及验证计划。工业相机场景拆解中的价格仅作为合成比较字段，库存仅作为流程演示字段；证据冲突时以用户提供的最新可核验资料为准。
